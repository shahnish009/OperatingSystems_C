Compile using 'make' command.
This will create an executable named 'nshah27_proj1'

To RUN, use the below format:

nshah27_proj1 H C 1


The extra argument through the command line is 1 (one). That value is used for indenting the output properly and to make it easy to read.


Thanks,
Nishant
